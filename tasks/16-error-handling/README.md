# Task 16: Error Handling

In JavaScript, errors can happen at runtime. Using `try...catch` allows you to handle these gracefully.
You can also throw custom errors using the `throw` keyword, or by extending the built-in `Error` class.

## Requirements
Implement the functions in `solution.js` to practice throwing and catching errors.
